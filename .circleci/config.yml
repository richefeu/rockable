# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/configuration-reference
version: 2.1

jobs:
  gen-doc:
    docker:
      - image: python:3.7
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: pip install -r sphinxdoc/requirements.txt
      - run:
          name: Build docs
          command: cd sphinxdoc/ && make html
      - persist_to_workspace:
          root: sphinxdoc/build
          paths: html

  docs-deploy:
    docker:
      - image: node:8.10.0
    steps:
      - add_ssh_keys:
          fingerprints:
            - "c9:4c:6b:6b:67:07:40:97:6e:97:2b:2f:14:ab:55:c7"
      - checkout
      - attach_workspace:
          at: sphinxdoc/build
      - run:
          name: Install and configure dependencies
          command: |
            npm install -g --silent gh-pages@2.0.1
            git config user.email "ci-build@klukas.net"
            git config user.name "ci-build"
      - run:
          name: Deploy docs to gh-pages branch
          command: gh-pages --dist sphinxdoc/build/html

workflows:
  version: 2.1
  build:
    jobs:
      - gen-doc
      - docs-deploy:
          requires:
            - gen-doc
          filters:
            branches:
              only: main


