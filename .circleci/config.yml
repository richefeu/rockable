# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/configuration-reference
version: 2.1

jobs:
  gen-doc:
    docker:
      - image: python:3.7
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: pip install -r sphinxdoc/requirements.txt
      - run:
          name: Build docs
          command: cd sphinxdoc/ && make html
      - persist_to_workspace:
          root: sphinxdoc/build
          paths: html

  docs-deploy:
    docker:
      - image: node:8.10.0
    steps:
      - checkout
      - attach_workspace:
          at: sphinxdoc/build
      - run:
          name: Install and configure dependencies
          command: |
            npm install -g --silent gh-pages@2.0.1
            git config user.email "richefeu@gmail.com"
            git config user.name "richefeu"
      - add_ssh_keys:
          fingerprints:
            - "c2:87:a0:78:24:cc:65:f6:5f:dd:79:66:d0:29:29:2c"
      - run:
          name: Deploy docs to gh-pages branch
          command: gh-pages --dotfiles --message "[skip ci] Updates" --dist sphinxdoc/build/html

workflows:
  version: 2.1
  build:
    jobs:
      - gen-doc
      - docs-deploy:
          requires:
            - gen-doc
          filters:
            branches:
              only: main


